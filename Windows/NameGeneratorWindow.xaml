<Window x:Class="AGR_Project_Manager.Windows.NameGeneratorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔤 Name Generator" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundPrimary}"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Стиль для заголовков блоков -->
        <Style x:Key="BlockHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <!-- Стиль для заголовков подблоков -->
        <Style x:Key="SubBlockHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundTertiary}"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>

        <!-- Стиль для элементов ComboBox -->
        <Style x:Key="DarkComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{DynamicResource ComboBoxBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimary}"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}"
                                BorderThickness="0">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboBoxItemHighlighted}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ComboBoxItemSelected}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для тёмного ComboBox -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource ComboBoxBackground}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItemStyle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>

                            <!-- Основной фон -->
                            <Border x:Name="Border"
                                    Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4"/>

                            <!-- Кнопка выпадающего списка -->
                            <ToggleButton x:Name="ToggleButton"
                                          Grid.ColumnSpan="2"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="Transparent">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Grid.Column="1" 
                                                           Text="▼" 
                                                           Foreground="{DynamicResource ComboBoxArrow}"
                                                           FontSize="10"
                                                           HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <!-- Контент (для не-редактируемого) -->
                            <ContentPresenter x:Name="ContentSite"
                                              Grid.Column="0"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              IsHitTestVisible="False"/>

                            <!-- TextBox для редактируемого ComboBox -->
                            <TextBox x:Name="PART_EditableTextBox"
                                     Grid.Column="0"
                                     Margin="3,3,23,3"
                                     Padding="{TemplateBinding Padding}"
                                     Background="Transparent"
                                     Foreground="{TemplateBinding Foreground}"
                                     BorderThickness="0"
                                     CaretBrush="{DynamicResource ForegroundPrimary}"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"
                                     Visibility="Hidden"
                                     Focusable="True"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Center"/>

                            <!-- Выпадающий список -->
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                      SnapsToDevicePixels="True">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource ComboBoxBackground}"
                                            BorderBrush="{DynamicResource BorderPrimary}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Margin="0,2,0,0">
                                        <ScrollViewer SnapsToDevicePixels="True"
                                                      VerticalScrollBarVisibility="Auto"
                                                      MaxHeight="300">
                                            <StackPanel IsItemsHost="True" 
                                                        KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource BorderHover}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{DynamicResource AccentPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="{DynamicResource BackgroundSecondary}" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="300"/>
                    <ColumnDefinition Width="40"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="400"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Выбор проекта -->
                <TextBlock Grid.Column="0" 
                           Text="Проект:" 
                           Foreground="{DynamicResource ForegroundSecondary}" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="ProjectComboBox"
                          Grid.Column="1"
                          Style="{StaticResource DarkComboBoxStyle}"
                          FontSize="14"
                          SelectionChanged="ProjectComboBox_SelectionChanged">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" 
                                       Foreground="{DynamicResource ForegroundPrimary}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Выбор района -->
                <TextBlock Grid.Column="3" 
                           Text="Район:" 
                           Foreground="{DynamicResource ForegroundSecondary}" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="DistrictComboBox"
                          Grid.Column="4"
                          Style="{StaticResource DarkComboBoxStyle}"
                          FontSize="13"
                          IsEditable="True"
                          IsTextSearchEnabled="True"
                          IsTextSearchCaseSensitive="False"
                          StaysOpenOnEdit="True"
                          TextSearch.TextPath="Name"
                          SelectionChanged="DistrictComboBox_SelectionChanged"
                          PreviewTextInput="DistrictComboBox_PreviewTextInput">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Number}" 
                                           Foreground="{DynamicResource CodeText}" 
                                           FontFamily="Consolas"
                                           FontWeight="Bold"
                                           Width="55"
                                           Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding Name}" 
                                           Foreground="{DynamicResource ForegroundPrimary}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <!-- Отображение кода района -->
                <StackPanel Grid.Column="6" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Код:" 
                               Foreground="{DynamicResource ForegroundTertiary}" 
                               Margin="0,0,8,0"
                               VerticalAlignment="Center"/>
                    <Border Background="{DynamicResource BackgroundPrimary}" 
                            CornerRadius="4" 
                            Padding="15,8"
                            MinWidth="70">
                        <TextBlock x:Name="DistrictCodeText"
                                   Text="----"
                                   Foreground="{DynamicResource CodeText}"
                                   FontFamily="Consolas"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>
                    </Border>
                </StackPanel>

                <!-- Информация -->
                <StackPanel Grid.Column="8" Orientation="Horizontal">
                    <TextBlock Text="💡 Клик по строке = копирование" 
                               Foreground="{DynamicResource ForegroundDisabled}" 
                               VerticalAlignment="Center"
                               FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Основной контент -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Блок 1: Геометрия -->
                <Border Grid.Column="0" 
                        Background="{DynamicResource BackgroundSecondary}" 
                        CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="📐 Геометрия" Style="{StaticResource BlockHeaderStyle}"/>

                        <TextBlock Text="High-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="GeometryHighPolyList"/>

                        <TextBlock Text="Low-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="GeometryLowPolyList"/>
                    </StackPanel>
                </Border>

                <!-- Блок 2: Материалы -->
                <Border Grid.Column="1" 
                        Background="{DynamicResource BackgroundSecondary}" 
                        CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="🎨 Материалы" Style="{StaticResource BlockHeaderStyle}"/>

                        <TextBlock Text="High-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="MaterialsHighPolyList"/>

                        <TextBlock Text="Low-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="MaterialsLowPolyList"/>
                    </StackPanel>
                </Border>

                <!-- Блок 3: Текстуры -->
                <Border Grid.Column="2" 
                        Background="{DynamicResource BackgroundSecondary}" 
                        CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="🖼️ Текстуры" Style="{StaticResource BlockHeaderStyle}"/>

                        <TextBlock Text="High-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="TexturesHighPolyList"/>

                        <TextBlock Text="Low-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="TexturesLowPolyList"/>
                    </StackPanel>
                </Border>

                <!-- Блок 4: Коллизии и Освещение -->
                <Border Grid.Column="3" 
                        Background="{DynamicResource BackgroundSecondary}" 
                        CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="💥 Коллизии и Освещение" Style="{StaticResource BlockHeaderStyle}"/>

                        <TextBlock Text="Коллизии" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="CollisionsList"/>

                        <TextBlock Text="Освещение" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="LightingList"/>
                    </StackPanel>
                </Border>

                <!-- Блок 5: FBX и Архивы -->
                <Border Grid.Column="4" 
                        Background="{DynamicResource BackgroundSecondary}" 
                        CornerRadius="8" Margin="5" Padding="15">
                    <StackPanel>
                        <TextBlock Text="📦 FBX и Архивы" Style="{StaticResource BlockHeaderStyle}"/>

                        <TextBlock Text="FBX High-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="FbxHighPolyList"/>

                        <TextBlock Text="FBX Low-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="FbxLowPolyList"/>

                        <TextBlock Text="Архивы High-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="ArchivesHighPolyList"/>

                        <TextBlock Text="Архив Low-Poly" Style="{StaticResource SubBlockHeaderStyle}"/>
                        <ItemsControl x:Name="ArchiveLowPolyList"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>