<Window x:Class="AGR_Project_Manager.Windows.ArchiveBuilderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="📦 Archive Builder" Height="700" Width="900"
        MinHeight="600" MinWidth="750"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundPrimary}"
        Loaded="Window_Loaded">

    <Window.Resources>
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="{DynamicResource BackgroundTertiary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Проект -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Проект" Foreground="{DynamicResource ForegroundSecondary}" FontSize="12" Margin="0,0,0,6"/>
            <ComboBox x:Name="ProjectComboBox"
          Background="{DynamicResource ComboBoxBackground}"
          Foreground="{DynamicResource ForegroundPrimary}"
          BorderBrush="{DynamicResource BorderPrimary}"
          Padding="10,8"
          SelectionChanged="ProjectComboBox_SelectionChanged">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource ForegroundPrimary}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>

        <!-- Папка проекта -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Папка проекта на диске (корень со структурой V2)"
                       Foreground="{DynamicResource ForegroundSecondary}" FontSize="12" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="ProjectPathTextBox" Grid.Column="0"
                         Background="{DynamicResource BackgroundTertiary}" Foreground="{DynamicResource ForegroundPrimary}"
                         BorderBrush="{DynamicResource BorderPrimary}" Padding="10,8" IsReadOnly="True"/>
                <Button Grid.Column="1" Content="📂 Выбрать..." Margin="8,0,0,0" Padding="12,8"
                        Background="{DynamicResource ButtonBackground}" Foreground="{DynamicResource ForegroundPrimary}"
                        Cursor="Hand" Click="BrowseProjectPath_Click"/>
                <Button Grid.Column="2" Content="🔍 Сканировать" Margin="8,0,0,0" Padding="12,8" FontWeight="SemiBold"
                        Background="{DynamicResource AccentPrimary}" Foreground="{DynamicResource ForegroundPrimary}"
                        Cursor="Hand" Click="Scan_Click"/>
            </Grid>
        </StackPanel>

        <!-- Дерево архивов -->
        <Border Grid.Row="2" Background="{DynamicResource BackgroundSecondary}" CornerRadius="8" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TreeView x:Name="ArchivesTreeView" BorderThickness="0" Background="Transparent"/>
            </ScrollViewer>
        </Border>

        <!-- Кнопки сборки -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" VerticalAlignment="Center"
                       Foreground="{DynamicResource ForegroundSecondary}" FontSize="12"/>
            <Button Grid.Column="1" Content="📦 Собрать все архивы" Padding="18,10" FontWeight="SemiBold"
                    Background="{DynamicResource AccentPrimary}" Foreground="{DynamicResource ForegroundPrimary}"
                    Cursor="Hand" Click="BuildAll_Click"/>
        </Grid>
    </Grid>
</Window>
